#include <iostream>
#include <cmath>
int main(){
    double x = 3;
    double y = 4;
    double z;
}

    //z = std::max(x, y);
    //z = std::min(x, y);
    //z = std::sqrt(x);
    //z = std::round(4.5); outputs 5
    //z = std::ceil(4.1); outputs 5
    //z = std::floor(4.9); outputs 4
    //z = std::abs(-4.9); outputs 4.9
    //z = std::pow(x, y); outputs 81      3^4 = 81
    //z = std::fmod(10, 3); outputs 1      10/3 = 3 remainder 1
